# A-to-do-list
displays a menu with options to view tasks, remove tasks and exit. allows users to view their tasks, add new tasks and remove existing task. handles invalid inputs and provides feedback to the user.
